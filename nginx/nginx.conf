events {}

http {
    upstream backend {
        server server-a:8000;
        server server-b:8000;
    }

    server {
        listen 80;

        location / {
            proxy_pass http://backend;
        }
    }
}